<h2>{{title}}</h2>
<p>Posted by {{User.user_name}} on {{createdAt}}</p>
<p>{{content}}</p>

<h3>Comments</h3>
{{#each Comments}}
<p>{{content}} - {{User.user_name}}</p>
{{else}}
<p>No comments yet.</p>
{{/each}}

{{#if loggedIn}}
<form action="/api/comments" method="POST">
  <textarea name="content" rows="4" cols="50" placeholder="Add a comment"></textarea><br>
  <input type="hidden" name="blogpost_id" value="{{id}}">
  <button type="submit">Submit Comment</button>
</form>
{{else}}
<p>Please <a href="/login">login</a> to leave a comment.</p>
{{/if}}
